# Assignment-2---simpleChat
To run the server, use EchoConsole
An optional command-line argument of a port number can be provided
To run the client, use ClientConsole
A loginID must be provided as the first command-line argument
An optional command-line argument of hostname and port number can be provided

